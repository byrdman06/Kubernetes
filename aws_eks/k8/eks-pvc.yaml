apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: eks-pvc
  namespace: web
spec:
  storageClass: ""
  resources:
    requests:
      storage: 1Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce